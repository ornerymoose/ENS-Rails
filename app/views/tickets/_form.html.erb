<%= form_for(@ticket) do |f| %>
  <% if @ticket.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ticket.errors.count, "error") %> prohibited this ticket from being saved:</h2>

      <ul>
      <% @ticket.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :property_id %><br>
    <%#= f.number_field :property_id %>
    <%#= collection_check_boxes(:ticket, :property_ids, Property.all, :id, :name) do |b| %>
    <%= hidden_field_tag :ticket, :property_ids %>
    <%= collection_select(:ticket, :property_ids, Property.all, :id, :name, prompt: true) %>
      
  </div>
  <div class="field">
    <%= f.label :event_status %><br>
    <%= f.text_field :event_status %>
  </div>
  <div class="field">
    <%= f.label :customers_affected %><br>
    <%= f.text_field :customers_affected %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
